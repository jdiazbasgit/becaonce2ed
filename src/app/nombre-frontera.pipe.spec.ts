import { NombreFronteraPipe } from './nombre-frontera.pipe';

describe('NombreFronteraPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreFronteraPipe();
    expect(pipe).toBeTruthy();
  });
});
