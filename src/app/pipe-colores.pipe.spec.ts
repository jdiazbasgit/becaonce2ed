import { PipeColoresPipe } from './pipe-colores.pipe';

describe('PipeColoresPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeColoresPipe();
    expect(pipe).toBeTruthy();
  });
});
