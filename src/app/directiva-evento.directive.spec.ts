import { DirectivaEventoDirective } from './directiva-evento.directive';

describe('DirectivaEventoDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaEventoDirective();
    expect(directive).toBeTruthy();
  });
});
