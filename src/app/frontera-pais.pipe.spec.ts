import { FronteraPaisPipe } from './frontera-pais.pipe';

describe('FronteraPaisPipe', () => {
  it('create an instance', () => {
    const pipe = new FronteraPaisPipe();
    expect(pipe).toBeTruthy();
  });
});
