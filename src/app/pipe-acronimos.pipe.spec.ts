import { PipeAcronimosPipe } from './pipe-acronimos.pipe';

describe('PipeAcronimosPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeAcronimosPipe();
    expect(pipe).toBeTruthy();
  });
});
