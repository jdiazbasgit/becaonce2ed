import { FronterasPipePipe } from './fronteraspPipe.pipe';

describe('FronterasPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FronterasPipePipe();
    expect(pipe).toBeTruthy();
  });
});
