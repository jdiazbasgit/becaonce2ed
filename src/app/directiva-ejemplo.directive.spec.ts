import { DirectivaEjemploDirective } from './directiva-ejemplo.directive';

describe('DirectivaEjemploDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaEjemploDirective();
    expect(directive).toBeTruthy();
  });
});
