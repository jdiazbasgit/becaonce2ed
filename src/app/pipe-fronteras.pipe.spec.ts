import { PipeFronterasPipe } from './pipe-fronteras.pipe';

describe('PipeFronterasPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFronterasPipe();
    expect(pipe).toBeTruthy();
  });
});
