import { FronteraPaiesePipe } from './frontera-paiese.pipe';

describe('FronteraPaiesePipe', () => {
  it('create an instance', () => {
    const pipe = new FronteraPaiesePipe();
    expect(pipe).toBeTruthy();
  });
});
