import { PipeSelectPipe } from './pipe-select.pipe';

describe('PipeSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
